using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class stage01 : PStage
{

    protected override void Ini(int n)
    {
        base.Ini(n);


    }


    // Start is called before the first frame update
    void Start()
    {
        Ini(1);
        Player_Appear();

    }

    // Update is called once per frame
    void Update()
    {
        


    }
}
